# H1 Compression-Gated ORB — gate report

_Generated by `scripts/04_compression_orb.py`._

**Verdict: NO-GO — WF-MCPT p=0.192; PBO=0.440**

## Numbers

- best params: `{'or_bars': 3, 'buffer_ticks': 4, 'compression_q': 0.33, 'stop_mult': 0.5}`  (honest N = 18)
- armed-day fraction: q=0.2: 24%, q=0.33: 37%, q=0.4: 44%
- trades (position changes): **1,350** (min-trades gate 200)
- annualized Sharpe: **1.16** (1-tick) / **1.09** (2-tick); PF 1.06
- leg P&L (log, 1-tick): long +0.2183 / short +0.1857 → two-sided ✔
- in-sample MCPT p = **0.0040** (500 perms; only 1 permutation beat the real strategy)
- walk-forward MCPT p = **0.1921**; DSR = **0.9883**; PBO = **0.4397**; bootstrap CI excludes 0: True

## Why NO-GO (three independent confirmations)

H1 looked alive in-sample — it **passes** IS-MCPT (p=0.004), clears 200 trades, and the pooled
`min(long, short) > 0` check called it "two-sided". But the out-of-sample and robustness gates
all reject it, and they agree:

1. **WF-MCPT p = 0.192** — re-optimized walk-forward, the OOS curve is *indistinguishable from
   permuted noise* (gate needs < 0.01). The parameters do not transfer forward.
2. **PBO = 0.44** — the in-sample-best parameter set lands in the bottom OOS half **44%** of the
   time (gate needs < 0.20). That is the signature of parameter overfitting, not a stable edge.
3. **Per-year leg gate: FAIL** — the apparent two-sidedness is a 2022 artifact (see below).

An adversarial code audit (5 independent agents tracing the real ns-resolution data) found
**no lookahead / leak** in either the arming signal or the intraday breakout — so the 1.1
in-sample Sharpe is *real*, which means the OOS failure is genuine overfitting/regime-dependence,
exactly what the gates exist to catch.

## Per-year leg decomposition (`scripts/04b_h1_leg_decomposition.py`, net 2-tick)

The pooled two-sided check (`min(pnl_long, pnl_short) > 0`) is too weak. Decomposed by year:

| year | long | short | both |
|---|---|---|---|
| 2019 | +0.0025 | +0.0064 | +0.0089 |
| 2020 | +0.0560 | −0.0116 | +0.0443 |
| 2021 | +0.0426 | +0.0235 | +0.0662 |
| 2022 | +0.0407 | **+0.1241** | +0.1648 |
| 2023 | +0.0315 | −0.0079 | +0.0236 |
| 2024 | +0.0277 | −0.0145 | +0.0131 |
| 2025 | −0.0001 | +0.0396 | +0.0395 |
| 2026 | +0.0052 | +0.0155 | +0.0208 |

- The **short leg books 71% of its total in 2022** (the bear year) and is **negative in 3 of 8
  years**. Drop 2022 and net-2-tick Sharpe collapses **1.09 → 0.79**.
- The new **per-year leg gate FAILS** (short positive in only 5/8 years; 71% concentrated in
  2022) where the old pooled check said PASS. This gate (`edge/validation/leg_gate.py`) is now
  the standard two-sided honesty test, applied **before** the expensive gate stack.

**Conclusion:** the "compression → expansion" story is not a stable, transferable, two-sided edge;
the in-sample result was overfit parameter selection plus 2022-bear concentration in the short leg.
Honest NO-GO. No threshold was loosened.

Equity curve: `reports/04_eq_compression_orb.png`.